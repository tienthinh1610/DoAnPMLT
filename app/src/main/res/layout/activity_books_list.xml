<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#CCFDF2"
    tools:context=".activityAdmin.BooksListActivity">
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbarbookslist"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#0A332A"
        app:titleMarginStart="60dp"
        app:navigationIcon="@drawable/ic_arrow_back_white"
        app:titleTextColor="@color/white"
        app:title="Danh sách sản phẩm" >

        <ImageButton
            android:id="@+id/imgbt_add"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginEnd="10dp"
            android:background="@null"
            android:contentDescription="@string/app_name"
            android:minWidth="48dp"
            android:minHeight="48dp"
            android:onClick="addbook"
            android:src="@drawable/ic_daucong" />
    </androidx.appcompat.widget.Toolbar>
    <ListView
        android:layout_marginTop="5dp"
        android:id="@+id/lvbookslist"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
</LinearLayout>